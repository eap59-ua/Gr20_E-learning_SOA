# ====================================================================
# CoursePurchaseProcessService — Configuración
# SEGÚN ENUNCIADO (sección 7.2.2): el flow de compra implementa Saga
# por coreografía. Las URLs de los servicios coordinados son
# configurables para permitir despliegues local / staging / prod.
# ====================================================================

# HTTP Listener (puerto distinto al CertificateService = 8081)
http.host=0.0.0.0
http.port=8088

# Base de datos MySQL — convención del grupo
db.host=localhost
db.port=3306
db.name=elearning_purchases
db.user=mule
db.password=mule123

# CourseService (REST, sección 5.1)
course.host=localhost
course.port=8082
course.basePath=/api/v1

# FinancialGatewayService (REST externo, sección 5.2)
# En desarrollo apuntamos al mock local; en prod sería
# https://payment-gateway.example.com/api
payment.host=localhost
payment.port=8086
payment.basePath=/api

# EnrollmentService (REST, sección 5.1)
enrollment.host=localhost
enrollment.port=8083
enrollment.basePath=/api/v1

# EmailNotificationService (REST, sección 5.1)
notification.host=localhost
notification.port=8085
notification.basePath=/api/v1
