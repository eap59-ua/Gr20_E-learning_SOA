# ====================================================================
# CertificateService — Configuración de propiedades
# SEGÚN ENUNCIADO (sección 7.1.2): TLS 1.2+ obligatorio en despliegue.
# Para entorno de desarrollo se usa HTTP plano en localhost.
# Credenciales unificadas con el resto del grupo (chat 15/05/2026).
# ====================================================================

# HTTP Listener
http.host=0.0.0.0
http.port=8087

# Base de datos MySQL — convención del grupo
db.host=localhost
db.port=3306
db.name=elearning_certificates
db.user=mule
db.password=mule123

# Endpoint del EmailNotificationService (REST, sección 5.1)
notification.host=localhost
notification.port=8085
notification.basePath=/api/v1

# Lógica de negocio
# Nota mínima de aprobado (sección 9.1 paso 4: "Verificar requisitos de certificación")
certificate.passingGrade=5.0

# Repositorio de almacenamiento de certificados (referencia lógica)
certificate.repositoryPrefix=REPO-CERT-
