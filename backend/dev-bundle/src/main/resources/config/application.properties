# ============================================================================
# Bundle de desarrollo — fichero de propiedades VACÍO
# ============================================================================
# Los XMLs de este bundle tienen los valores HARDCODED (puertos, BBDD, URLs)
# porque cada servicio tendría llaves distintas y se pisarían entre sí si
# se cargaran como propiedades globales. Para producción y entrega final,
# cada servicio en su carpeta de soap-services/ y rest-services/ tiene su
# propio application.properties.
#
# Este fichero está aquí solo porque algún XML del bundle aún hace referencia
# a 'config/application.properties' y Mule falla si no lo encuentra.
# ============================================================================
